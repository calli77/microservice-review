version: '3.8'
services:
  app:
    build: .
    ports:
      - "3556:3556"
    environment:
      NODE_ENV: production
      MONGO_URI: ${MONGO_URI}  # L'URI MongoDB depuis le fichier .env
    volumes:
      - .:/app
